# Authentication Documentation

## Overview

This document provides an overview of the authentication system used in the application.

## Authentication Flow

The authentication flow is handled by the `angular-oauth2-oidc` library.

## Keycloak Configuration

The Keycloak configuration is defined in the `auth.service.ts` file.

## Silent Refresh

Silent refresh is used to avoid automatic disconnection.